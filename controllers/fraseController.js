const fraseModel = require('../models/frase');
const traducaoModel = require('../models/traducao');

const fraseController = {
    salvarFrase: async(req, res) => {
        
    }
}

module.exports = fraseController;